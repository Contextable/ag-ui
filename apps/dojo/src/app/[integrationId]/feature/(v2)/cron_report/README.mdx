# Cron Report

Fixed-schema A2UI demo showing recent cron job results as rich cards.
Uses OpenClaw's native automation/cron capabilities to retrieve run history.
Each card displays: start time, duration, model used, tokens consumed, and a free-text status summary.
